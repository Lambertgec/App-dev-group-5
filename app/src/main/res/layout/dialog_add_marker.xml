<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/input_building_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/building_name" />

    <EditText
        android:id="@+id/input_room_number"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/room_number_optional" />

    <!-- Only visible when a room number is entered -->
    <EditText
        android:id="@+id/input_level"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/level_e_g_0_1_2"
        android:inputType="numberSigned"
        android:visibility="gone" />

</LinearLayout>